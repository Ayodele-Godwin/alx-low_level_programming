#include <stdio.h>
/**
 * main - a program to print a line with the put function
 * Return: 0 (when its return then it is a success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle.");
return (0);
}
